import {createTheme} from "@mui/material";

const AppTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#3396ff',
        }
    },
});

export default AppTheme;