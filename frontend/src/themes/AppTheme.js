import {createTheme} from "@mui/material";

const AppTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#90c1aa',
        }
    },
});

export default AppTheme;